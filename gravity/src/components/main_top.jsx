import * as _ from './main_top.js'

export const MainTop = () => {
    return(
        <_.top>
            <_.logo>
                자유낙하 시각화
            </_.logo>
            <_.logButton onClick={()=>{}}>
                로그인
            </_.logButton>
        </_.top>
    )
}